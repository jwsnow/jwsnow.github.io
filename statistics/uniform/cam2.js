questionID[currentProblem]='cam2';
question[currentProblem]='The angle at which the crankshaft in an engine stops spinning is uniformly distributed from 0 to 360 degrees.<p>What is the probability that the shaft stops below 100 degrees? \
<br>(Enter an exact fraction or a decimal rounded to four places.) \
';
type[currentProblem]='N';
answer[currentProblem]='100/360';
hint[currentProblem]='The density curve for this uniform random variable is a horizontal line whose height is such that the area under the line over the interval from 0 to 360.<p>Find the height of this line and then multiply by the width of the interval in question.';
epsilon[currentProblem]=.00011;
notes[currentProblem]='notes/L12-uniform.pdf';
currentProblem++;

